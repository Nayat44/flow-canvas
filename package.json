{
  "name": "flow-canvas",
  "version": "0.1.0",
  "private": true,
  "description": "A flow map that lives inside the app it documents — Claude Code skill plus a portable React Flow implementation",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "install-skills": "./install.sh",
    "typecheck": "tsc -p tsconfig.json",
    "validate": "python3 skills/flow-canvas/scripts/validate.py examples/flows.example.json"
  },
  "peerDependencies": {
    "@dagrejs/dagre": "^1.1.4",
    "@xyflow/react": "^12.11.0",
    "html-to-image": "^1.11.13",
    "react": ">=18",
    "react-dom": ">=18"
  }
}
